Метод сопряженных градиентов, для симметричной положительно определённой матрицы размера n гарантированно сходится к точному решению в общем случае за n итераций (есть погрешность представления вещественных чисел, может потребоваться и больше итераций), на каждой своей итерации требует умножения матрицы A на вектор. Таким образом, сложность
одной итерации пропорциональна числу ненулевых элементов матрицы A, итого O(mn), где m - кол-во ненулевых элементов.

По памяти метод сопряжённых градиентов требует хранение 3 векторов, значит, его сложность O(n)
